# CLAUDE.md — Minha Saúde

App mobile de nutrição e acompanhamento de saúde (inspirado no MyFitnessPal, sem usar esse nome), com diferencial de análise de exames de sangue e bioimpedância. Projeto de portfólio **e** com usuários reais desde a Fase 1 — não é só um exercício isolado, então segurança e LGPD não são "depois". Público brasileiro, app em português. Desenvolvedor solo, sem prazo fixo, orçamento de infra R$0 por enquanto (ver ADR-0007).

## Arquitetura
- Monorepo: pnpm workspaces + Turborepo (`apps/api`, `apps/mobile`, `packages/shared`, `packages/config`).
- Backend: NestJS + TypeORM + PostgreSQL, padrão Repository (domínio desacoplado do ORM).
- Mobile: React Native com Expo (Expo Router, EAS Build).
- Tipos compartilhados: OpenAPI como fonte da verdade do contrato HTTP + codegen de client para o mobile; `packages/shared` para enums/schemas Zod de domínio.
- Hosting (orçamento zero, ver ADR-0007): API no Render free tier, Postgres no Supabase free tier, arquivos de exame no Cloudflare R2 free tier. Tudo atrás de env vars — trocar de provedor pago não deve exigir mudança de código.
- Ver decisões completas e alternativas descartadas em `docs/adr/`.
- Modelagem de dados em `docs/database-schema.md`, contrato de API em `docs/api-contract.md`.

## Comandos
```bash
pnpm install                          # instala tudo no monorepo
pnpm --filter api start:dev           # roda API local
pnpm --filter api migration:run       # aplica migrations
pnpm --filter api migration:generate  # gera migration a partir de mudança de entidade
pnpm --filter mobile start            # roda Expo dev server
turbo run lint test build             # pipeline completo (usado no CI)
```

## Regras de segurança e LGPD (obrigatórias em toda sessão — projeto tem usuários reais desde a Fase 1, segurança nunca é "depois")
- Dados de saúde (exames, medidas corporais) são **dados pessoais sensíveis** — qualquer feature nova que os toque precisa checar consentimento ativo (`consents`) antes de processar.
- Nunca logar payload de exame, senha, token ou dado de perfil de saúde — usar redact no Pino.
- Nunca sobrescrever `exam_results.raw_value`/`raw_unit` — são o dado extraído originalmente, imutável. Valores confirmados vão em `confirmed_*`.
- `body_measurements.source` é obrigatório e nunca "normalizado" entre aparelhos de bioimpedância diferentes — não implementar comparação direta entre fontes distintas sem aviso explícito na UI.
- Qualquer texto de UI relacionado a resultado de exame/índice calculado deve ser informativo, nunca diagnóstico ou prescritivo (RDC 657/2022 — ver `docs/product-plan.md` seção 5). Não implementar lógica do tipo "se X > limite, recomendamos Y".
- Toda calculadora de índice de saúde (`packages/shared/src/calculators/`) precisa gravar `calculator_version` no resultado — histórico não pode mudar retroativamente se a fórmula for corrigida.
- Senhas com argon2 (nunca bcrypt puro sem custo adequado, nunca MD5/SHA sozinho). JWT de acesso curto + refresh token rotacionado e revogável.
- Arquivos de exame: sempre via URL assinada temporária, nunca URL pública direta ao bucket.
- Atribuição obrigatória de dados: TACO/Unicamp e Open Food Facts (licença ODbL) — não remover créditos ao alterar telas de "sobre".
- Upload de exame: limite de negócio de 1 por `exam_type` a cada 30 dias por usuário (controle de custo de IA), além de rate limiting técnico geral.
- Baseline de segurança obrigatória desde a Fase 1 (não é opcional para depois): `helmet` no NestJS, `nestjs-throttler` global (mais restritivo em `/auth/*` e `/exams`), ValidationPipe com `whitelist`+`forbidNonWhitelisted` em todo endpoint novo, guard de auth por padrão (rotas públicas são allowlist explícita, nunca o contrário), `pnpm audit`/Dependabot rodando em CI.
- Endpoints de exportação (`GET /v1/me/export`) e exclusão (`DELETE /v1/me`) de conta precisam ser **funcionais de verdade na Fase 1**, não stub — há usuários reais desde o início.

## Convenções de código
- Seguir `rules/common/coding-style.md`: imutabilidade, arquivos pequenos (200-400 linhas típico, 800 máximo), Repository pattern, validação em toda borda do sistema com Zod/class-validator.
- Nomes de entidades/variáveis em inglês; textos de UI em português (pt-BR).
- Migrations do TypeORM sempre geradas, nunca `synchronize: true` fora de teste local isolado.
- Testes: TDD para lógica de negócio crítica (cálculo de metas, normalização de exames, calculadoras de índices) — não exigir 80% de cobertura em CRUD trivial, priorizar qualidade sobre número.

## O que este projeto NÃO faz (fora de escopo até indicação contrária)
- Diagnóstico médico ou recomendação de tratamento/dosagem.
- Comparação direta de bioimpedância entre aparelhos diferentes.
- Import em massa do Open Food Facts (busca é sob demanda — ver ADR-0004).
- Funcionalidades sociais, gráficos avançados, leitor de código de barras, registro de treinos — reservado para Fase 6 (`docs/roadmap.md`).

## Active Context (última sessão: 2026-09-25)
- **Fase 3 COMPLETA (issues #17-#21), tudo em `main` e publicado, e as 9 telas validadas visualmente** (ver detalhes e os 3 bugs que isso revelou mais abaixo). Detalhes por issue em `docs/backlog-fase3.md`.
- **Issue #21 concluída (2026-09-24)**: aba "Peso" (`(app)/weight.tsx`) com registro rápido (reaproveita `weightEntrySchema` do shared + `AuthTextField`/`PrimaryButton` do onboarding), histórico e gráfico de linha. **Gráfico feito com `react-native-svg` 15.15.4 + componente próprio, NÃO com `react-native-gifted-charts`**: a gifted-charts exige `react-native-linear-gradient` (módulo nativo bare, fora do ecossistema Expo) e a única via de validação hoje é o Expo web; a versão do svg é a que o SDK 57 fixa em `bundledNativeModules.json`. Confirmado que o bundle web compila sem erro de resolução. A geometria mora em `features/weight/chart-geometry.ts` (função pura, 11 testes): eixo x escala por **tempo decorrido, não por índice** (espaçar por índice mentiria sobre o ritmo), eixo y invertido, e casos de divisão por zero (pesos iguais, medições no mesmo instante) cobertos. Listagem fixa `source=manual` — a tela nunca mistura fontes de bioimpedância. Na API: `BodyMeasurementResponseDto` + `PaginatedBodyMeasurementResponseDto` (`weightKg` é `string` porque coluna `numeric` volta string pelo TypeORM). Limpeza junto: aba/tela "Explore" e a marca "Expo Starter" eram sobra do template e saíram; a barra agora é Diário + Peso. **Ícone da aba Peso ainda é o PNG do template** — falta o set de ícones da identidade Gota Vital (mesma pendência das fontes).
- **Bug real do guard raiz, corrigido (2026-09-24, commit `4a557b8`)**: `_layout.tsx` usava `hasActiveGoal = goalQuery.isSuccess`, então **qualquer** falha da busca de meta (rede, CORS, 5xx) era lida como "usuário ainda não tem meta" e jogava alguém que já concluiu o onboarding de volta no fluxo, pedindo o consentimento LGPD de novo. A regra virou `resolveRootRoute()` em `features/onboarding/root-route.ts` (função pura, 10 testes): só 404 leva ao onboarding, qualquer outro erro cai em `GoalUnavailableView` (tela de retry). Cobre também a query desabilitada do React Query v5, que fica com `isPending: true` sem usuário autenticado.
- **Gotcha de ambiente que custou um ciclo (2026-09-24)**: para validar o app web no navegador, apontar o Expo para a **API local**, nunca para produção. O `CORS_ORIGINS` de produção não inclui `http://localhost:8081` — o preflight volta `204` sem `access-control-allow-origin` e o navegador bloqueia *todos* os requests, o que na UI aparece como erro genérico de rede ("Verifique sua conexão") e parece bug de feature. Stack local: `docker compose up -d postgres` → `pnpm --filter api migration:run` → `pnpm --filter api start:dev` → `cd apps/mobile && npx expo start --web --port 8081` (o `.env.local` já aponta pra `localhost:3000`, e o `.env` da API já tem `CORS_ORIGINS` com `:8081`). O `.env.example` ainda cita `19006`, porta antiga do Expo web. Cuidado: o token fica no `localStorage` da origem `:8081` e sobrevive à troca de API, deixando o app "autenticado" contra um backend que não conhece o usuário.
- **Vazamento de campo interno nas respostas — CORRIGIDO (2026-09-24, commit `b177b30`, em produção)**: os controllers retornavam a entidade crua do TypeORM, então o DTO documentava o contrato no Swagger mas não filtrava nada em runtime (`userId`, `ownerUserId`, `createdAt`/`updatedAt`, `externalId`, `barcode`, `rawPayload` viajavam pro cliente). Solução: helper `toDto`/`toDtoList` em `apps/api/src/common/serialization/to-dto.ts` (`plainToInstance` + `excludeExtraneousValues`) e `@Expose()` nos DTOs de resposta — **DTO virou a única fonte da verdade: campo não declarado não sai**, mesmo que alguém adicione coluna nova na entidade depois. O mapeamento fica no **controller**, não no service, pra manter o domínio desacoplado do transporte. Descartado `ClassSerializerInterceptor` + `@Exclude` nas entidades: sujaria o domínio com decorator de transporte, contra o Repository pattern.
  - **Ao criar endpoint novo**: declarar um DTO de resposta com `@Expose()` em cada campo (e `@Type(() => Dto)` em aninhado, senão o filtro não desce) e envolver o retorno com `toDto(...)`. Sem isso o endpoint volta a vazar.
  - **Por que importava de verdade**: `passwordHash` só era removido por um `destructuring` manual repetido em `getMe` e `exportData`, e a coluna **não** tem `select: false` — qualquer endpoint novo que devolvesse `User` vazaria o hash. Agora `/v1/me` passa por `MeResponseDto` e a proteção é estrutural.
  - **Exceção consciente**: `GET /v1/me/export` **não** passa pelo filtro — é o direito de portabilidade da LGPD, o usuário tem direito aos dados completos (inclusive o `profileSnapshot` das metas, que saiu de `/v1/goals/current` por nunca ter estado no contrato). Só o hash de senha fica fora, e há teste pra isso.
  - **`FoodResponseDto` passou a expor `source`**: não é campo interno, é a procedência do dado nutricional, necessária pra creditar TACO/Unicamp e Open Food Facts (ODbL). Quem apontou isso foi um e2e existente ao falhar — ele verificava que o servidor força `source=custom`, comportamento real que seguiria sem cobertura pelo contrato público.
  - **Regressão travada** em `apps/api/test/response-leak.e2e-spec.ts` (11 testes): varre a resposta em profundidade e falha se campo interno aparecer em qualquer nível, inclusive no alimento aninhado do diário; cobre também o caminho oposto (o filtro não cortou demais).
  - `reflect-metadata` virou `setupFiles` do jest da API — sem ele os decorators do `class-transformer` não funcionam em teste unitário puro.
  - **Ainda não expostos, de propósito** (decisão de produto, não vazamento): `fiberGPer100g` e `barcode` existem no banco e seguem invisíveis ao cliente.
- **Gotcha do graphify**: o extrator AST pula alguns `.tsx` com `AttributeError: 'list' object has no attribute 'startswith'` (aconteceu com `_layout.tsx` e `goal-unavailable-view.tsx`). É bug da ferramenta; os `.ts` entram normalmente. O vault fica atualizado mas não 100% completo para esses arquivos.
- **Issue #20 (diário alimentar + resumo diário) concluída (2026-09-18)**: retomada exatamente do ponto onde a sessão anterior parou. Correções na API primeiro: `DiaryService.getByDate`/`findOwnedOrThrow` ganharam `relations: { food: true }` (TypeORM 0.3 exige objeto, não array, nesta versão) — o bug real de antes (resposta sem nome do alimento) está corrigido; `create`/`update`/`copy` agora propagam a entidade `food` já buscada em `computeSnapshot` pra não disparar uma query extra. DTOs de resposta tipados criados: `PaginatedFoodResponseDto` (foods/search), `MacroTotalsDto`/`DiaryEntryResponseDto`/`DailySummaryResponseDto` (diary, com `food` aninhado e `meals` como objeto `{breakfast,lunch,dinner,snack}` em vez de `Record<MealType,_>` — mais previsível pro client gerado). Decorators `@ApiOkResponse`/`@ApiCreatedResponse`/`@ApiNoContentResponse` aplicados nos controllers de `foods` e `diary` (mesmo padrão das issues #18/#19, plugin do swagger continua desabilitado — ver gotcha antigo).
- **Achado real novo, corrigido**: `FoodResponseDto.brand` e `DiaryEntryResponseDto.portionId` (campos `string | null`) tinham decorator sem `type` explícito (`@ApiPropertyOptional()`/`@ApiProperty({ nullable: true })`) — o `@nestjs/swagger` reflete um union `string | null` como `Object` por não conseguir inferir o tipo primitivo via reflection, e o `orval` gerava um tipo placeholder inútil (`{[key: string]: unknown}`) em vez de `string | null` no client. Corrigido com `{ type: String, nullable: true }` explícito nos dois. Vale conferir esse padrão em qualquer DTO futuro com campo nullable de tipo primitivo.
- **Telas mobile novas**: `(app)/index.tsx` deixou de ser o placeholder "Welcome to Expo" e virou a tela real do diário (navegação entre dias, card de resumo consumido/meta/restante via `features/diary/macro-summary.tsx`, seções por refeição via `features/diary/meal-section.tsx` + `diary-entry-row.tsx`, "copiar dia anterior" só quando o dia atual está vazio). Tela nova `app/diary-entry.tsx` (fora do grupo `(app)`, registrada como `Stack.Screen` modal no `_layout.tsx` raiz dentro do mesmo `Stack.Protected` do app principal) cobre busca de alimento + criação e edição/remoção de entrada — reaproveita `AuthTextField`/`PrimaryButton` do onboarding. Sem tela de porções (ver nota acima sobre a API não expor `FoodPortion`).
- **Sem teste de tela no diário — por um diagnóstico que hoje se sabe errado** (ver o item do RTL mais abaixo): a cobertura ficou em `apps/api/src/diary/diary.service.spec.ts` (13 testes, incluindo os cenários de `relations`/food) e em `tsc --noEmit` limpo nos arquivos novos do mobile. Agora que o `render` assíncrono está entendido, as telas do diário **podem** ganhar teste de render — vale fazer junto da Fase 4.
- **Validação real feita nesta sessão**: API reiniciada localmente (`pnpm --filter api start:dev`, com watch), Swagger regenerado, client mobile regenerado (`pnpm --filter mobile generate:api`) e conferido campo a campo. `turbo run lint test build` limpo nos 4 packages (rodado por pacote, `--concurrency=1`, mesmo gotcha de sandbox de sempre). **Validação visual no browser não foi possível**: a extensão Claude in Chrome não estava conectada nesta sessão (`tabs_context_mcp` retornou "Browser extension is not connected"). Servidor Expo web ficou rodando em `http://localhost:8081` (API em `http://localhost:3000`) pro usuário conferir manualmente — não afirmar que a UI foi validada visualmente até isso acontecer de verdade.
- **Fase 1: essencialmente completa** (issues #1-#9). Issue #8 (observabilidade): logs de produção indo pro BetterStack (source `minhasaude-api`) via `@logtail/pino` (`apps/api/src/config/pino-transport.ts`), redact do pino-http valendo antes de qualquer transport — falta só o exception filter global custom (item pendente de baixo risco, ver `docs/backlog-fase1.md` issue #8; comportamento default do NestJS já não vaza stack trace pro cliente).
- **Fase 2 completa** (issues #10-#15, backlog e detalhes técnicos em `docs/backlog-fase2.md`): catálogo de calculadoras (`packages/shared/src/calculators/` — TMB, TDEE, macros, ajuste por objetivo), `body-measurements`, `goals` (TMB/TDEE/macros de verdade, histórico imutável), catálogo de alimentos + import TACO (582 itens, `pnpm --filter api seed:taco`), busca (trigram/unaccent + fallback Open Food Facts com cache), e `diary` (diário alimentar com snapshot imutável de macros, cópia de dia, resumo vs. meta). Todo módulo novo: consentimento LGPD quando toca dado de saúde, incluído em `GET /v1/me/export`, testado com unitários + e2e.
- **Achado crítico corrigido em produção (2026-09-18)**: `packages/shared` nunca tinha sido compilado de verdade (`build` era um `echo` placeholder desde a Fase 1) — `node dist/main.js` (como produção roda) não resolvia `@minhasaude/shared` em runtime. Corrigido: `tsconfig.build.json` + `main`/`types` apontando pra `dist/`, e o `buildCommand` do serviço no Render atualizado via API pra `pnpm exec turbo run build --filter=@minhasaude/api` (builda o shared antes, via `dependsOn` do `turbo.json`). Validado: deploy manual ficou `live`, `/health` e `/docs` responderam 200.
- **Auto-deploy no push: resolvido e confirmado** (2026-09-18) — GitHub App do Render instalado (antes só havia login OAuth, sem o app de verdade, por isso o webhook nunca disparava). Testado com o push real do commit da issue #15: deploy disparou sozinho, ficou `live`, sem precisar do `POST .../deploys` manual. Fase 2 (issues #10-#15) está publicada em produção de verdade.
- **Gotcha de plataforma**: NestJS 12, `@nestjs/jwt` 12, `@nestjs/typeorm` 12 são ESM-only e quebram Jest — projeto fixado em `11.x`/legacy de cada pacote Nest. Verificar antes de atualizar qualquer `@nestjs/*`.
- **Produção real**: https://minhasaude-api.onrender.com, Postgres no Supabase (`sa-east-1`, session pooler), bucket R2 `minhasaude-exams`.
- **Fase 3 (app mobile) completa**: backlog em `docs/backlog-fase3.md`, issues **#17-#21** no GitHub (setup, client+auth, onboarding, diário+resumo, peso+gráfico). Decisões de stack travadas: `orval` para o client OpenAPI (ADR-0003 atualizada) e `NativeWind` para estilização (ADR-0008).
- **Identidade visual "Gota Vital" aprovada (2026-09-18)**: paleta com nome de comida brasileira (Areia/Mamão/Couve/Jabuticaba/Maracujá, hex + variante dark), tipografia em 3 papéis (Fredoka/Work Sans/JetBrains Mono) e símbolo (gota + traço de batimento). Detalhes e hex exatos na memória `project_identidade_visual_gota_vital`; tokens de cor já aplicados em `apps/mobile/src/global.css` + `tailwind.config.js`. Fontes (Fredoka/Work Sans/JetBrains Mono) ainda não carregadas no app — pendente para quando houver telas de UI reais.
- **Issue #17 concluída (2026-09-18)**: `apps/mobile` scaffoldado de verdade (Expo SDK 57 + Expo Router + TypeScript, a partir do template oficial `create-expo-app`, adaptado pro monorepo). NativeWind 4.2.7 configurado (`babel.config.js`, `metro.config.js`, `tailwind.config.js`, `src/global.css`) — validado que uma `className` real compila no bundle (cores da identidade aparecem no CSS gerado pelo `expo export`). `@minhasaude/shared` resolvido pelo Metro e pelo Jest (teste em `src/integration/shared-package.spec.ts`). ESLint via `@minhasaude/config/eslint-preset.cjs` + `eslint-config-expo/flat` (precisou filtrar a config do preset compartilhado pra não colidir o registro do plugin `@typescript-eslint` com o do `eslint-config-expo` — ver `apps/mobile/eslint.config.js`). `eas.json` com profiles development/preview/production criado, mas nenhum build real disparado (precisa de conta Expo — passo manual do dev). `turbo run lint test build` roda limpo nos 4 packages do monorepo.
- **Gotcha de plataforma (mobile)**: o isolamento estrito de node_modules do pnpm quebra resolução de plugins do babel do ecossistema Expo/RN (ex.: `@babel/plugin-transform-react-jsx` não resolvia a partir de `babel-preset-expo`). Corrigido com `nodeLinker: hoisted` (afeta `apps/api`/`packages/shared` também, mas ambos foram revalidados — testes e build seguem passando).
- **pnpm atualizado de 9.15.0 → 12.4.2 (2026-09-18)**, decisão final depois de pesquisar a fundo (ver `pnpm/pnpm#12880`): a 9.15.0 tinha um bug real e determinístico no linker `hoisted` no Windows (race no `fs.hard-link-dir` — um pacote sendo copiado apagava a pasta temporária de um pacote vizinho instalando ao mesmo tempo, causando `ERR_PNPM_ENOENT` intermitente). Corrigido pelo próprio time do pnpm no commit `2f2bdaf` (~27/08/2026) — confirmado via comparação de commits que a **linha 9.x nunca vai receber esse patch** (branch já divergiu 3713 commits, efetivamente abandonada); a correção só existe a partir de `11.27.0`/`12.x`. Atualizado também o **binário global** (`npm install -g pnpm@12.4.2` — o `pnpm` global da máquina tinha prioridade no PATH sobre o corepack).
- **pnpm 12 mudou onde várias configs vivem** (WARN explícito ao instalar): `pnpm-workspace.yaml` agora é a única fonte pra `overrides` (não lê mais `package.json` → `pnpm.overrides`) e pra `nodeLinker` (não lê mais `.npmrc` → `node-linker`). Migrado: `.npmrc` removido, `pnpm-workspace.yaml` agora tem `nodeLinker: hoisted` + `overrides` (incluindo `js-yaml: ">=4.3.2 <5"` — apertado porque `>=4.3.2` sozinho resolve pra v5, que quebra o import ESM do `orval`).
- **pnpm 12 também trouxe duas políticas de supply-chain novas, ambas exigindo ação na primeira instalação**: `minimumReleaseAge` (recusa por padrão pacotes publicados muito recentemente — o próprio pnpm auto-populou `minimumReleaseAgeExclude` no `pnpm-workspace.yaml` pros pacotes do Expo publicados no mesmo dia) e scripts de build/postinstall de dependências passam a vir bloqueados por padrão (`pnpm approve-builds --all` liberou `argon2`, `@parcel/watcher`, `esbuild`, `unrs-resolver`, `@scarf/scarf` — sem isso o `argon2` não compila e a API quebra em runtime). Isso também ficou registrado em `pnpm-workspace.yaml` (`allowBuilds`).
- **Gotcha de sandbox (não é bug de código)**: rodar `turbo run lint test build` com concorrência total às vezes crasha (`ERR_PNPM`-like spawn errors, stack overrun no ESLint) neste ambiente por disputa de recursos — cada task sozinha ou com `--concurrency=1` sempre passa limpo. Se acontecer de novo, reduzir concorrência antes de assumir regressão real.
- **Issue #18 concluída (2026-09-18)**: client HTTP gerado via `orval` (`apps/mobile/orval.config.ts`, roda com `pnpm --filter mobile generate:api`) — gera hooks do TanStack Query pra API inteira (auth, diary, foods, goals, body-measurements, consents), não só auth, em `src/api/generated/` (nunca editado à mão). Mutator customizado em `src/api/http-client.ts`: axios com token guardado via `expo-secure-store` (`src/api/token-storage.ts`), renovação automática num 401 (fila compartilhada entre chamadas concorrentes) e sessão limpa quando o refresh também falha. `AuthProvider`/`useAuth` em `src/features/auth/auth-context.tsx` (login, registro com login encadeado, logout). Guard de rota via `Stack.Protected` no `_layout.tsx` raiz — telas movidas pra grupos `(app)`/`(auth)`. Telas de login/registro com a paleta Gota Vital. Testes: login feliz, credencial inválida, sessão persistida, registro encadeado, logout (`auth-context.spec.tsx`) e renovação automática de token simulada com `axios-mock-adapter` (`http-client.spec.ts`) — 10 testes, todos passando. **Ajuste feito na API** (fora do escopo original da issue, mas necessário pro client vir tipado): controllers de auth ganharam `@ApiOkResponse`/`@ApiCreatedResponse`/`@ApiNoContentResponse` explícitos (não dá pra confiar no plugin `@nestjs/swagger` — ver gotcha abaixo) e um `RegisterResponseDto` novo. `turbo run lint test build` roda limpo nos 4 packages.
- **Gotcha real do plugin `@nestjs/swagger` (NÃO habilitar `"plugins": ["@nestjs/swagger"]` no `nest-cli.json`)**: ele injeta `@ApiResponse` automático nos controllers, e o `require()` que gera pra referenciar o tipo de resposta usa um path absoluto de `src/` (não `dist/`) — quebra em runtime via `nest start --watch` (webpack), reproduzido de forma determinística (não é race). Solução adotada: decorators `@ApiOkResponse`/`@ApiCreatedResponse` explícitos em cada controller, sem o plugin.
- **Issue #19 concluída (2026-09-18)**: onboarding (consentimento LGPD → perfil → peso → cálculo de meta), grupo de rota `(onboarding)` novo. Guard raiz agora é 3 vias: `useGoalsControllerGetCurrent` decide entre `(app)`/`(onboarding)`/`(auth)` (404 = sem meta ainda → onboarding). `packages/shared/src/onboarding/`: enums runtime (SEXES/ACTIVITY_LEVELS/GOALS, derivados dos tipos já existentes de `calculators/types.ts`), `CURRENT_PRIVACY_POLICY_VERSION`, e schemas Zod (`profileFormSchema`, `weightEntrySchema`) espelhando os limites de validação da API — zod virou dependência real do shared. **Ajuste na API**: `GoalsController.getCurrent`/`recalculate` ganharam `GoalTargetResponseDto` + `@ApiOkResponse` (todos os módulos de Fase 2 — me/goals/body-measurements/consents — ainda retornam `void` no client gerado por falta de decorator; só tipei os 2 que o onboarding precisa de verdade). Telas usam chips (`SelectChips`) em vez de picker nativo — evita dependência nativa nova nesta fase.
- **~~Gotcha de teste (mobile)~~ — DIAGNÓSTICO ERRADO, RESOLVIDO (2026-09-25)**: por três issues (#19, #20, #21) este arquivo registrou que `@testing-library/react-native` era "não-confiável neste ambiente" e falhava "de forma não-determinística", e testes de render foram removidos por isso. **Estava errado.** A causa é determinística e trivial: no **RTL 14 o `render` é `async`** (`async function render(...)` em `dist/render.js`) porque ele aguarda o `act` internamente. Chamado sem `await`, devolve uma **Promise** — que é `typeof 'object'`, tem `Object.keys` vazio e não tem as queries, daí o `"getByText is not a function"`; e o `screen` ainda não está ligado, daí o `"render function has not been called"`.
  - **Como escrever teste de tela aqui**: `await render(<Componente />)` e depois `screen.getByText(...)` (o retorno do `await` também traz as queries). `userEvent.press` também é `await`. Validado com componente real do projeto, com estado, `SafeAreaProvider` e interação — **5 execuções seguidas, 100% verde**. Não há instabilidade nenhuma.
  - Para telas que usam `SafeAreaProvider`, passar `initialMetrics` no provider evita depender de medição real.
  - **Lição que vale além deste caso**: "ambiente instável" foi aceito como explicação três vezes sem ninguém abrir o `dist/` da biblioteca. O sintoma ("is not a function") apontava para uma mudança de API, não para flakiness.
- **Validação de ponta a ponta feita pela API local (2026-09-24)**, já que o navegador não estava disponível: onboarding completo (consent → perfil → peso → meta), busca de alimento, criar/editar/copiar/remover entrada do diário (o `food.name` volta certo — o bug da #20 está de fato corrigido), `GET /v1/me/export` incluindo `diaryEntries`, e 3 pesos em datas diferentes listados por `source=manual`.
- **Validação visual das 9 telas FEITA (2026-09-24)**, com a extensão Claude in Chrome enfim conectada (antes `tabs_context_mcp` só respondia "Browser extension is not connected"; destravou depois de reiniciar o Chrome, e `list_connected_browsers` é o diagnóstico melhor que a mensagem genérica). Telas conferidas em dark mode: login, registro, consentimento, perfil, peso (onboarding), resumo da meta, diário, peso (app) e o modal de adicionar alimento. Fluxos exercitados de ponta a ponta: registro com login encadeado, onboarding completo, busca (8 variedades de banana da TACO), adicionar entrada, navegação entre dias, registrar peso, logout. Conferido na mão que a meta bate (Mifflin-St Jeor, 33 anos/172 cm/68,5 kg → TMB 1600, ×1,55 = 2480 kcal) e que o aviso da RDC 657/2022 está na tela de meta.
- **A varredura visual achou 3 bugs que nenhum teste pegava** (commits `978c17b` e `6015128`, em produção):
  1. **Contraste ilegível no dark mode**: 7 componentes usavam `bg-white` fixo com `text-grafite` — e `grafite` vira `#f5eae0` (creme) no escuro, ou seja texto claro sobre fundo branco. Atingia diário, peso e 2 telas do onboarding, desde as issues #19/#20. Corrigido com o token novo `--color-superficie` (branco no claro, `#2a211b` no escuro) em `global.css` + `tailwind.config.js`. **`bg-white` não deve voltar a ser usado — sempre `bg-superficie`.**
  2. **O gráfico de peso não desenhava nada**: o componente só montava o SVG depois que `onLayout` entregasse a largura, e no react-native-web esse evento não chega. Confirmado no DOM: o elemento media 1838px, o estado seguia 0, e a página tinha zero `polyline`/`circle`. Agora a largura inicial vem de `useWindowDimensions` e o `onLayout` só refina. **Lição: os 11 testes de geometria passavam o tempo todo — a matemática estava certa, o componente é que nunca era montado.**
  3. **A barra de abas bloqueava o topo de TODAS as telas na web**: o container da TabList é `position: absolute` com `width: 100%`, então a faixa transparente de 1920×76 em volta da pílula central engolia cliques (`elementFromPoint` na posição do "Sair" devolvia a barra). Logout e setas de navegação entre dias eram inalcançáveis — por isso nunca tinham sido exercitados. Corrigido com `pointerEvents="box-none"`.
- **Varredura estática junto**: zero cores hardcoded nas telas (`bg-white`, `text-gray` etc. não existem mais). Os hex restantes vivem só em resquícios do template (`animated-icon.tsx`, `themed-text.tsx`, `constants/theme.ts`) — a barra de abas ainda é pintada pelo tema do template, não pela paleta Gota Vital.
- **Contas de teste no banco local** (docker, não é produção): `e2e-1790272811@local.test` e `sweep-ui-01@local.test`, senha `SenhaTeste123!` nas duas.
- **Exclusão de conta — RESOLVIDA (2026-09-25, commit `9863c47`)**: `AccountPurgeService.purgeDueAccounts()` apaga a linha de `users` e o `ON DELETE CASCADE` leva junto as 6 tabelas dependentes (conferidas no banco, não presumidas). `consents` e `account_deletion_requests` não têm FK, então ficariam para trás: o consent é **anonimizado** (limpa `ip_address`/`user_agent`) e mantido como prova, decisão tomada com o usuário. Agendado por `AccountPurgeScheduler` (`@nestjs/schedule` fixado em 5.x — a 12.x é ESM-only e quebraria o Jest). Passos idempotentes em vez de transação: se morrer no meio, a próxima execução retoma. 9 testes unitários + 6 e2e contra banco real. **Falta**: documentar o prazo de 30 dias no texto da política de privacidade (Fase 6).
- **Três observações de UX da varredura, não corrigidas**: a busca de alimento mostra só o nome, sem kcal/100g (o dado já existe no `FoodResponseDto`, dá pra comparar opções); o modal de adicionar alimento não tem botão de fechar visível (na web o "voltar" resolve, num device não); ícone da aba Peso ainda é o PNG do template.
- **Higiene pendente**: issues **#20 e #21 continuam abertas no GitHub** apesar de prontas e publicadas (e a #9 parece concluída de fato — helmet, throttler, ValidationPipe, guard por padrão, Dependabot e `audit` em CI estão todos no lugar); `docs/backlog-fase1.md` tem **todos os checkboxes desmarcados** apesar da fase estar completa, o que engana quem chega novo.
- **ROADMAP REORDENADO (2026-09-25, decisão do usuário)**: a publicação **saiu da frente**. Nova ordem: **Fase 4 = refundação da interface** (nova), **Fase 5 = exames e métricas** (o diferencial), **Fase 6 = publicação** (era a 4), **Fase 7 = evolução** (era a 6). Motivo: o diferencial do produto estava *depois* da publicação, o que significaria publicar — e pedir a 12 testers 14 dias de uso — de um diário alimentar comum. A publicação também custa US$ 124 que o orçamento R$ 0 não cobre, então deixá-la por último transforma bloqueio em espera que não atrapalha. Ver a nota de reordenação no topo da Fase 4 em `docs/roadmap.md`.
- **Fase 4 (interface) vem ANTES dos exames de propósito**: exames são a maior superfície de UI do projeto (upload, revisão de valores, resultados, índices); construí-los sobre a base atual significaria refazer acessibilidade duas vezes. Backlog completo com as issues **#30-#34** em `docs/backlog-fase4.md` (numeração real no GitHub — os PRs do Dependabot consumiram #22-#29).
- **Auditoria de acessibilidade (2026-09-25) que motivou a fase**: `apps/mobile/src` tem **39 elementos tocáveis e 0 com `accessibilityLabel`/`accessibilityRole`/`accessibilityState`**, 0 tratam escala de fonte, chips do onboarding têm ~36 dp e botões ~44 dp de alvo de toque (mínimo recomendado: 48 dp). Norma de referência adotada: **ABNT NBR 17060:2022** (54 requisitos alinhados ao WCAG, específica para apps móveis, base do art. 63 da LBI 13.146/2015 — ressalva: o art. 63 fala em *sites*, então hoje é postura e preparação, não obrigação líquida).
- **Portão de orçamento novo, descoberto no replanejamento**: a extração de exames por IA **cobra por chamada**, e o orçamento é R$ 0. O limite de 1 upload por `exam_type` a cada 30 dias reduz o custo, não zera. O projeto tem **dois** portões de dinheiro, e este vem antes do das lojas. Decidir antes de iniciar a Fase 5: nível gratuito enquanto durar, ou tratar como portão.
- **Contradição em aberto sobre o nome**: `product-plan.md` seção 1 confirma "Minha Saúde"; a seção 4 do mesmo arquivo recomenda `NutriTrilha` ou `Mapa Vital` e alerta que o atual é genérico demais (prefeituras e planos de saúde usam). Precisa de decisão antes de investir em marca, na Fase 6.
- **Próximo passo natural**: iniciar a Fase 4 pela issue #30 (componentes base acessíveis por construção) — é a fundação das outras quatro.

## Referências
- Plano completo: `docs/product-plan.md`
- ADRs: `docs/adr/`
- Roadmap: `docs/roadmap.md`
- Backlog Fase 1: `docs/backlog-fase1.md`
