{
  "name": "minha-saude",
  "private": true,
  "packageManager": "pnpm@9.15.0",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "build": "turbo run build",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "dev": "turbo run dev"
  },
  "devDependencies": {
    "turbo": "^2.3.0"
  },
  "pnpm": {
    "overrides": {
      "multer": ">=2.3.0",
      "js-yaml": ">=4.3.2",
      "path-to-regexp": ">=8.4.0",
      "lodash": ">=4.18.0"
    }
  }
}
